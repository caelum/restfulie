collection(@payments) do |collection|
  collection.values do |value|
    value.id payments_url
    value.title "Payments"
    value.updated Time.now
  end
  # collection.members do |member,payment|
  # end
end

