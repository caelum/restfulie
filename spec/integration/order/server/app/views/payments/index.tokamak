describe_collection(@payments) do |collection|
  collection.id = payments_url
  collection.describe_members do |member,payment|
    member.links << link( :rel => :approve, :href => approve_payment_url(payment.id) )
    member.links << link( :rel => :refuse, :href => refuse_payment_url(payment.id) )
  end
end

