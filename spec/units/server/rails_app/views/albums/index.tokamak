describe_collection(@albums) do |collection|
  collection.title = "Index Album feed spec"

  collection.describe_members(:namespace => "http://localhost/albums") do |member, album|
    partial("member", binding)
  end
end