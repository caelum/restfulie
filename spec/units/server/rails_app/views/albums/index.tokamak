describe_collection(@albums) do |collection|
  collection.title = "Index Album feed spec"
  
  collection.describe_members do |member, album|
    member.links << link(:rel => :artistis, :href => album_artists_url(album))
  end
end